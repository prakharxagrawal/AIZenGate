# Prometheus configuration for ZenGate development
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'zengate-gateway'
    scrape_interval: 5s
    static_configs:
      - targets: ['gateway:8080']
    metrics_path: '/metrics'
