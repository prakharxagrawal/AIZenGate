# 🧠 Brain Package Documentation

The `brain` package serves as the reasoning engine for ZenGate AI. It abstracts the complexity of the Google Generative AI SDK into a simple interface for the self-healer.

## Interface: `LLMClient`

The `LLMClient` is the primary entry point for interacting with the LLM.
